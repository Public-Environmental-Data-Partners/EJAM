# Installed EJAM data repository version marker

Text file marker used by EJAM to record which `ejamdata` release
supplied locally installed Arrow-format datasets.

## Format

A one-line text file installed in the package `data` directory.

## Source

`ejamdata` release metadata.
