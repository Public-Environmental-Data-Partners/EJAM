# lat_alias, lon_alias (DATA) Synonyms for lat and lon

lists of synonyms for 'latitude' and 'longitude' used when guessing
which column is what in user-provided tables of coordinates

## Usage

``` r
lat_alias
```

## Format

An object of class `character` of length 7.
