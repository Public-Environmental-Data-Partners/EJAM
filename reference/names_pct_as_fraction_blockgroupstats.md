# which indicators are percentages stored as 0-1 not 0-100, in blockgroupstats

which indicators are percentages stored as 0-1 not 0-100, in
blockgroupstats

## Usage

``` r
names_pct_as_fraction_blockgroupstats
```

## Format

An object of class `character` of length 51.
