# utility - estimate how large table might be that will be output of getblocksnearby()

utility - estimate how large table might be that will be output of
getblocksnearby()

## Usage

``` r
getblocks_predict_blocks_per_site(nsites, radius)
```

## Arguments

- nsites:

  count of sites

- radius:

  radius in miles

## Value

a number
