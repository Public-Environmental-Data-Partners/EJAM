# Utility used in app_server to predict ejamit and doaggregate runtimes

Utility used in app_server to predict ejamit and doaggregate runtimes

## Usage

``` r
predict_ejamit_runtime(rows, radius)
```

## Arguments

- rows:

  number of locations to be analyzed

- radius:

  buffer radius distance, in miles
