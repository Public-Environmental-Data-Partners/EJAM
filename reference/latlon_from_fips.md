# get approx centroid of each fips census unit

get approx centroid of each fips census unit

## Usage

``` r
latlon_from_fips(fips)
```

## Arguments

- fips:

  vector of census fips codes

## Value

table in [data.table](https://r-datatable.com) format with columns
ftype, fips, lat, lon
