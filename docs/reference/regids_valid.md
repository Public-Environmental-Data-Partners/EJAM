# validate regids by checking if they are in the database being used currently

validate regids by checking if they are in the database being used
currently

## Usage

``` r
regids_valid(regid)
```

## Arguments

- regid:

  vector of IDs like testinput_regid

## Value

logical vector same length as input
