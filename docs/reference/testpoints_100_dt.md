# test points data.frame with columns sitenumber, lat, lon

test points data.frame with columns sitenumber, lat, lon

## Usage

``` r
testpoints_100_dt
```

## Format

An object of class `data.table` (inherits from `data.frame`) with 100
rows and 4 columns.
