# Format predicted runtime in compact human-readable text

Format predicted runtime in compact human-readable text

## Usage

``` r
speed_format_seconds(seconds)
```

## Arguments

- seconds:

  predicted seconds.

## Value

Character string.
