# Classify FIPS inputs for runtime prediction

Classify FIPS inputs for runtime prediction

## Usage

``` r
speed_fips_analysis_subtype(fips)
```

## Arguments

- fips:

  vector of FIPS codes.

## Value

A single subtype string such as `"city"`, `"county"`, `"mixed"`, or
`"unknown"`.
