# test points data.frame with columns note, lat, lon

test points data.frame with columns note, lat, lon

## Usage

``` r
testpoints_bad
```

## Format

An object of class `data.frame` with 9 rows and 3 columns.

## Details

examples of test points for testing functions that need lat lon, with
some invalid or tricky cases like rural (no blocks nearby), outside US,
on edge of two states, NA values, etc.
