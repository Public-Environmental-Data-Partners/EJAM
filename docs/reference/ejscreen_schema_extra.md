# EJSCREEN schema-only extra field names

EJSCREEN schema-only extra field names

## Usage

``` r
ejscreen_schema_extra
```

## Format

An object of class `character` of length 3.

## Details

A character vector of EJSCREEN export field names that are schema extras
rather than standard EJAM indicator rnames.
