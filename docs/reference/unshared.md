# UTILITY - see what is only in x or y but not both - just like setdiff except for y,x and also x,y

setdiff2 aka unshared just shows which elements are in one and only one
of the sets x and y

## Usage

``` r
unshared(x, y)
```

## Examples

``` r
EJAM:::unshared(1:4, 3:8)
```
