% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_bg_geodata.R
\name{calc_bg_geodata}
\alias{calc_bg_geodata}
\title{Blockgroup geography fields used by the EJSCREEN/EJAM pipeline}
\usage{
calc_bg_geodata(
  yr,
  bgfips = NULL,
  states = NULL,
  bg_geodata = NULL,
  existing_blockgroupstats = NULL,
  reuse_existing_if_missing = FALSE,
  allow_partial_reuse = FALSE,
  download = is.null(bg_geodata),
  geodata_source = c("tigerweb", "tiger"),
  tigerweb_base_url = "https://tigerweb.geo.census.gov/arcgis/rest/services/TIGERweb",
  tiger_base_url = "https://www2.census.gov/geo/tiger",
  download_dir = file.path(tempdir(), "ejam_tiger_bg"),
  download_timeout = 3600,
  download_retries = 2,
  pipeline_dir = NULL,
  save_stage = FALSE,
  stage_format = c("csv", "rds", "rda", "arrow"),
  overwrite = TRUE,
  pipeline_storage = c("auto", "local", "s3"),
  validation_strict = TRUE
)
}
\arguments{
\item{yr}{ACS/TIGER vintage year.}

\item{bgfips}{optional vector of blockgroup FIPS codes that define the
desired output universe.}

\item{states}{optional vector of 2-digit state FIPS codes. If NULL and
\code{bgfips} is supplied, states are inferred from \code{bgfips}.}

\item{bg_geodata}{optional already-loaded geography table.}

\item{existing_blockgroupstats}{optional existing blockgroupstats-like table
used only as an explicit fallback source.}

\item{reuse_existing_if_missing}{logical. If TRUE, reuse \code{arealand} and
\code{areawater} from \code{existing_blockgroupstats} when TIGER data are missing.
This requires the old and new \code{bgfips} sets to match unless
\code{allow_partial_reuse} is TRUE.}

\item{allow_partial_reuse}{logical. If TRUE, allow fallback reuse when the
old and new \code{bgfips} sets differ, with a warning and possible missing
values.}

\item{download}{logical. If TRUE, download Census blockgroup geography
attributes when \code{bg_geodata} is not supplied.}

\item{geodata_source}{preferred Census source. \code{"tigerweb"} queries only
needed attributes from Census TIGERweb and is the default. \code{"tiger"}
downloads TIGER/Line blockgroup zip files.}

\item{tigerweb_base_url}{Census TIGERweb REST base URL.}

\item{tiger_base_url}{Census TIGER base URL used by the TIGER/Line zip
fallback.}

\item{download_dir}{local folder for downloaded TIGER/Line zip files.}

\item{download_timeout}{timeout in seconds for Census downloads.}

\item{download_retries}{number of retries after a failed Census download.}

\item{pipeline_dir}{folder for saving the pipeline stage.}

\item{save_stage}{logical, whether to save the \code{bg_geodata} stage.}

\item{stage_format}{file format for saved stages: \code{"csv"}, \code{"rds"},
\code{"rda"}, or \code{"arrow"}.}

\item{overwrite}{logical, whether to overwrite an existing saved stage.}

\item{pipeline_storage}{stage storage backend: \code{"auto"}, \code{"local"}, or
\code{"s3"}.}

\item{validation_strict}{logical passed to \code{\link[=ejscreen_pipeline_save]{ejscreen_pipeline_save()}}.}
}
\value{
data.table with \code{bgfips}, \code{arealand}, \code{areawater}, optional
\code{intptlat}, \code{intptlon}, and compatibility-only \code{area}.
}
\description{
Blockgroup geography fields used by the EJSCREEN/EJAM pipeline
}
\details{
\code{bg_geodata} stores Census/TIGER blockgroup geography attributes
needed by later pipeline stages. By default the function queries Census
TIGERweb for only the blockgroup attributes EJAM needs, and it discovers the
Census Block Groups layer for the requested ACS/TIGER vintage instead of
assuming that TIGERweb layer numbers are stable across years. \code{arealand} and
\code{areawater} are Census square-meter fields and should be used for area
weighting and area-derived checks. The legacy \code{area} column is retained only
for compatibility with older EJScreen/EJAM tables and should not be used for
calculations.
}
\keyword{internal}
