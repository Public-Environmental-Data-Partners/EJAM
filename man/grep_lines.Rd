% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_PACKAGE_DEV.R
\name{grep_lines}
\alias{grep_lines}
\title{utility - Helper for find_in_files()}
\usage{
grep_lines(
  pattern,
  x,
  ignore.case = TRUE,
  ignorecomments = FALSE,
  value = TRUE
)
}
\arguments{
\item{pattern}{regular expression to look for}

\item{x}{character vector to search, typically one element per line}

\item{ignore.case}{logical passed to \code{\link[=grepl]{grepl()}}}

\item{ignorecomments}{if \code{TRUE}, lines beginning with \verb{#} are excluded}

\item{value}{if \code{TRUE}, return matching lines; otherwise return a logical vector}
}
\value{
Character vector of matching lines if \code{value = TRUE}, otherwise a
logical vector the same length as \code{x}. Returned values are named with line
numbers where applicable.
}
\description{
Internal helper used by \code{\link[=find_in_files]{find_in_files()}} to search text that is
already in memory, such as the output of \code{\link[=readLines]{readLines()}}.
}
\details{
Search an in-memory character vector line by line

This is somewhat like grepv() but with these options:
option to return numbers of the elements or line numbers as names of the output vector
option to ignore commented-out lines of code (if searching with in lines of code)
option to return just the matching part of the element or line instead of the whole line if desired.ut

use grepl to find all members of character vector z where the character string "h" appears in the string
but the string does not start with zero or more spaces followed by the character "#"
}
\examples{

EJAM:::grep_lines("x",  c("x", "y", "has any x x xxxxx"))

xx = c("   ej", "ej", "#ej", "   #ej", "asdf#ej", "   asdf#ej", "#   ej", "#   xej", "x#  ej", "  x#ej")

 cbind(xx, EJAM:::grep_lines("ej", xx, ignorecomments = TRUE,  value = FALSE))
 cbind(xx, EJAM:::grep_lines("ej", xx, ignorecomments = FALSE, value = FALSE))

 cbind(  EJAM:::grep_lines("ej", xx, ignorecomments = TRUE,    value = TRUE))
 cbind(  EJAM:::grep_lines("ej", xx, ignorecomments = FALSE,   value = TRUE))

}
\seealso{
\code{\link[=find_in_files]{find_in_files()}} \code{\link[=grepn]{grepn()}} \code{\link[=grepns]{grepns()}} \code{\link[=grepls]{grepls()}}
}
\keyword{internal}
