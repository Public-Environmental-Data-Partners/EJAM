% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_blockgroupstats_from_tract_data.R
\name{calc_blockgroupstats_from_tract_data}
\alias{calc_blockgroupstats_from_tract_data}
\title{utility to calculate annually for EJSCREEN the updated ACS data available at only tract resolution (\% disability & language detail)}
\usage{
calc_blockgroupstats_from_tract_data(
  yr,
  tables = c("B18101", "C16001", "B27010"),
  formulas = NULL,
  dropMOE = TRUE,
  acs_raw = NULL,
  tract_weight_source = c("decennial2020", "acs")
)
}
\arguments{
\item{yr}{endyear of ACS 5-year survey to use, inferred if omitted}

\item{tables}{ACS tract tables used for tract-derived indicators, typically
\code{"B18101"}, \code{"C16001"}, and \code{"B27010"} for disability, detailed language,
and health insurance.}

\item{formulas}{default includes formulas for disability-related and language-related indicators
calculated from tract-level ACS variables. This is a vector of string formulas.}

\item{dropMOE}{logical, whether to drop not retain the margin of error information for each ACS variable}

\item{acs_raw}{optional raw ACS table list or \code{bg_acs_raw} pipeline object
previously created by \code{\link[=download_bg_acs_raw]{download_bg_acs_raw()}}. If supplied, no ACS download
is performed for tract-resolution tables.}

\item{tract_weight_source}{source for blockgroup-to-tract apportionment
weights. \code{"decennial2020"} uses 2020 Decennial Census population weights,
matching the legacy EJSCREEN approach. \code{"acs"} uses same-vintage ACS
blockgroup population from \code{acs_raw} or downloads it when needed.}
}
\value{
data.table, one row per blockgroup (not tract)
}
\description{
utility to calculate annually for EJSCREEN the updated ACS data available at only tract resolution (\% disability & language detail)
}
\details{
This is now typically orchestrated by \code{\link[=calc_ejscreen_dataset]{calc_ejscreen_dataset()}} and the
staged pipeline recipe/config helpers documented in
\code{data-raw/run_ejscreen_pipeline_*.R}.

Relies on the function get_acs_new() which is available from the package ACSdownload (on github) as ACSdownload::get_acs_new()

Needs Census API key for \code{\link[tidycensus:get_decennial]{tidycensus::get_decennial()}}.

Takes some time to download data for every State!

First get tract counts, then apportion every tract count into blockgroup
counts (the tract count times the blockgroup's share of the tract
population, \code{bgwt}), then carry each tract's percentages onto its
blockgroups.

Apportioning applies to the language counts from C16001 (\code{lan_universe},
\code{lan_spanish}, etc.) exactly as it does to disability and health insurance.
\code{map_headernames} marks all of these as \code{"sum of counts"}, so
\code{\link[=doaggregate]{doaggregate()}} and \code{\link[=calc_acs_by_geography]{calc_acs_by_geography()}} add them up across
blockgroups. A tract total repeated on every blockgroup would be counted
once per blockgroup, about three times over, in every buffer, county, and
state total (issue EJAM#596). Apportioned counts add back up to the tract
total, to rounding, which is also how EPA's EJScreen block group layer
reports language counts.

Percentages such as \code{pctlan_spanish} are calculated at tract level and
repeated on each blockgroup in the tract. That equals the ratio of the
apportioned counts wherever \code{bgwt > 0}, and it keeps the tract rate on
zero-population blockgroups instead of reporting 0 there, the same
convention \code{pctdisability} uses. Apportioned counts are rounded to whole
persons after the percentages are set.

For ACS 2022 and later, Connecticut ACS tract FIPS use planning-region
county equivalents while 2020 Decennial blockgroup FIPS use the older county
equivalents. When \code{tract_weight_source = "decennial2020"} and \code{acs_raw} is
available, the function detects this mismatch and remaps the 2020
Decennial weights to the same ACS blockgroup suffixes, using same-vintage
ACS blockgroup population weights only for ambiguous or unmatched cases.
}
\keyword{internal}
