% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/URL_FUNCTIONS_part2.R
\name{url_county_equityatlas}
\alias{url_county_equityatlas}
\title{URL functions - Get URLs of useful report(s) on County containing the given fips from nationalequityatlas.org}
\usage{
url_county_equityatlas(
  fips = NULL,
  sitepoints = NULL,
  lat = NULL,
  lon = NULL,
  shapefile = NULL,
  as_html = FALSE,
  linktext = "County (Equity Atlas)",
  ifna = "https://nationalequityatlas.org",
  baseurl = "https://nationalequityatlas.org/research/data_summary",
  statereport = FALSE,
  ...
)
}
\arguments{
\item{fips}{vector of fips codes}

\item{sitepoints}{if provided and fips is NULL, gets county fips from lat,lon columns of sitepoints}

\item{lat, lon}{ignored if sitepoints provided, can be used otherwise, if shapefile and fips not used}

\item{shapefile}{if provided and fips is NULL, gets county fips from lat,lon of polygon centroid}

\item{as_html}{Whether to return as just the urls or as html hyperlinks to use in a DT::datatable() for example}

\item{linktext}{used as text for hyperlinks, if supplied and as_html=TRUE}

\item{ifna}{URL shown for missing, NA, NULL, bad input values}

\item{baseurl}{do not change unless endpoint actually changed}

\item{statereport}{Do not use directly. Used by \code{\link[=url_state_equityatlas]{url_state_equityatlas()}}.
if TRUE, gets report on enclosing State/DC/PR, not county.
if FALSE, returns NA when given a State fips, otherwise return report on enclosing county.}

\item{...}{unused}
}
\value{
vector of URLs to reports on enclosing counties (or generic link if necessary, like when input was a state fips)
}
\description{
URL functions - Get URLs of useful report(s) on County containing the given fips from nationalequityatlas.org
}
\examples{
 url_county_equityatlas(fips_counties_from_state_abbrev("DE"))
 # browseURL(url_county_equityatlas(fips_counties_from_state_abbrev("DE"))[1])

}
