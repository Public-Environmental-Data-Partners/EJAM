% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ejscreen_export.R
\name{add_ejscreen_map_fields}
\alias{add_ejscreen_map_fields}
\title{Add EJSCREEN map helper fields}
\usage{
add_ejscreen_map_fields(
  x,
  mapping_for_names = map_headernames,
  pctile_names = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{x}{data.frame with EJSCREEN-named percentile fields such as
\code{P_D2_NO2}.}

\item{mapping_for_names}{map_headernames-like crosswalk.}

\item{pctile_names}{optional vector of EJSCREEN percentile field names to
use. Defaults to all exported \code{P_...} fields known to \code{mapping_for_names},
plus any other exported fields whose names start with \code{P_}.}

\item{overwrite}{logical. If TRUE, recalculate existing \code{B_...} and \code{T_...}
fields from the matching percentile fields.}
}
\value{
data.frame with added or updated \code{B_...} and \code{T_...} fields.
}
\description{
Add EJSCREEN map helper fields
}
\details{
EJSCREEN app datasets include map helper fields that EJAM does not
otherwise need: \code{B_...} small-integer color-bin fields and \code{T_...} popup-text
fields. This helper creates those fields from exported \code{P_...} percentile
columns.

The bin logic is adapted from the obsolete \code{ejanalysis::assign.map.bins()}
helper, but implemented directly in EJAM. Percentiles must be on EJSCREEN's
0-100 scale, not 0-1. Current EJSCREEN services use popup text like
\code{"95 \%ile"}, so that is the text format used here.
}
\keyword{internal}
