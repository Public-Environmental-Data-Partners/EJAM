% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ejscreen_compare_geography_to_epa.R
\name{ejscreen_compare_geography_to_epa}
\alias{ejscreen_compare_geography_to_epa}
\title{Replication check: compare an EJAM by-geography table to an EPA EJScreen reference}
\usage{
ejscreen_compare_geography_to_epa(
  ejam_table,
  reference,
  ejam_fips_col = "tractfips",
  reference_fips_col = "STCNTR",
  reference_cols = NULL,
  pct_scale = 100
)
}
\arguments{
\item{ejam_table}{EJAM output (e.g. \code{acs_by_tract}) with \code{ejam_fips_col} and
EJAM \code{rname} indicator columns.}

\item{reference}{EPA reference table (e.g. the \code{attributes} from a
\code{EJScreen_Census/.../by_Tract} feature-server query) with \code{reference_fips_col}
and EPA ACS-style column names.}

\item{ejam_fips_col, reference_fips_col}{Id columns. Defaults \code{"tractfips"} and
\code{"STCNTR"} (EPA's full 11-digit tract GEOID field).}

\item{reference_cols}{EPA columns to compare; default is all non-id columns.}

\item{pct_scale}{Factor applied to EJAM percentage columns to match EPA's 0-100
scale. Default \code{100}.}
}
\value{
A data.frame, one row per compared indicator: \code{reference}, \code{rname}, \code{n},
\code{cor}, \code{median_absdiff} (sorted worst-agreement first), with attribute
\code{"n_joined"}.
}
\description{
Used by the annual-update "replication" steps to compare a pipeline output such
as \code{acs_by_tract} (or the per-block-group EJ-index percentile-rank / threshold
layers) against EPA's published EJScreen values. EPA exposes these as ArcGIS
\strong{FeatureServers} (e.g. the \code{EJScreen_Census} service's \code{by_Tract} layer), not
as archived CSVs in the S3 pipeline folders, so the EPA \code{reference} is typically
fetched live from a feature server rather than read from a stage file.
}
\details{
The comparison: (1) repair FIPS leading zeros on both id columns with
\code{\link[=fips_lead_zero]{fips_lead_zero()}}; (2) map EPA's ACS-style column names (e.g. \code{TOTALPOP},
\code{PCT_LOWINC}) to EJAM \code{rname}s with \code{\link[=fixcolnames]{fixcolnames()}}; (3) inner-join on the id;
(4) for each shared indicator report n, correlation, and median absolute
difference. EPA stores percentages as 0-100 while EJAM stores fractions 0-1, so
percentage indicators are scaled by \code{pct_scale} (default 100) before comparison.
This only checks indicators EJAM actually has; EPA's extra "extensive ACS"
demographics are ignored. It is meaningful only when the pipeline build and the
EPA reference are the same ACS vintage (e.g. both ACS 2018-2022).
}
\seealso{
\code{\link[=calc_acs_by_geography]{calc_acs_by_geography()}} \code{\link[=fixcolnames]{fixcolnames()}} \code{\link[=fips_lead_zero]{fips_lead_zero()}}
}
\keyword{internal}
