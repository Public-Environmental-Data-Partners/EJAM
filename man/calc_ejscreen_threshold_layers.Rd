% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ejscreen_threshold_layers.R
\name{calc_ejscreen_threshold_layers}
\alias{calc_ejscreen_threshold_layers}
\title{Build the EJSCREEN web-app "thresholds" layers (P1..P100 hit counts)}
\usage{
calc_ejscreen_threshold_layers(
  pctiles,
  id_col = "bgfips",
  layers = c("us_ejindexes", "state_ejindexes", "us_supplemental", "state_supplemental"),
  cols_us_ej = names_ej_pctile,
  cols_state_ej = names_ej_state_pctile,
  cols_us_supp = names_ej_supp_pctile,
  cols_state_supp = names_ej_supp_state_pctile,
  out_dir = NULL
)
}
\arguments{
\item{pctiles}{A data.frame, one row per block group, containing \code{id_col} and
the EJ-index percentile-rank columns -- for example the output of
\code{\link[=calc_ejscreen_export]{calc_ejscreen_export()}} (which holds \verb{P_D2_*} and \verb{P_D5_*}), or a merge of
the national and state exports so all four column sets are present.}

\item{id_col}{Name of the block-group id column to carry through. Default
\code{"bgfips"}.}

\item{layers}{Which layers to build; any of \code{"us_ejindexes"},
\code{"state_ejindexes"}, \code{"us_supplemental"}, \code{"state_supplemental"}.}

\item{cols_us_ej, cols_state_ej, cols_us_supp, cols_state_supp}{Character vectors
of the percentile-rank column names for each layer. Defaults are the EJAM
name vectors \link{names_ej_pctile}, \link{names_ej_state_pctile},
\link{names_ej_supp_pctile}, \link{names_ej_supp_state_pctile}.}

\item{out_dir}{Optional directory; if supplied, each layer is written there as
\verb{<layer>.csv}.}
}
\value{
A named list of data.frames, one per requested layer (the id column +
the percentile-rank columns + \code{P1}..\code{P100}).
}
\description{
Produces the four "threshold map widget" layers requested for the EJSCREEN web
app in issue #395: \code{us_ejindexes}, \code{state_ejindexes}, \code{us_supplemental}, and
\code{state_supplemental}. Each layer has one row per block group containing the
per-index percentile ranks plus 100 columns \code{P1}..\code{P100} giving, for that block
group, how many of its EJ-index percentile ranks land at each integer
percentile (e.g. if a block group is at the 4th percentile for \code{P_D2_OZONE}
and no other D2 index rank is 4, then \code{P4 = 1}).
}
\details{
Percentile ranks are rounded to integers and clamped into \verb{1..100} (a rank
that rounds to 0 is counted in \code{P1}, anything above 100 in \code{P100}, and \code{NA}
ranks are not counted), then the number of indexes at each integer percentile
is tallied per block group in a single pass over the data. For ranks already
in \verb{1..100} this equals the \code{\link[=colcounter]{colcounter()}} identity
\code{colcounter(x, k) - colcounter(x, k + 1)} (count at-or-above \code{k} minus count
at-or-above \code{k + 1}), i.e. it is the per-block-group analogue of the
\verb{EXCEED_COUNT_*} fields in \code{\link[=calc_ejscreen_export]{calc_ejscreen_export()}}.

The four layers differ only in which set of percentile columns they tabulate:
\itemize{
\item \code{us_ejindexes} -> \link{names_ej_pctile} (national D2 EJ indexes)
\item \code{state_ejindexes} -> \link{names_ej_state_pctile}
\item \code{us_supplemental} -> \link{names_ej_supp_pctile} (national supplemental, D5)
\item \code{state_supplemental} -> \link{names_ej_supp_state_pctile}
}

In the annual pipeline these layers are produced by the \code{ejscreen_export} stage
of \code{\link[=calc_ejscreen_dataset]{calc_ejscreen_dataset()}} when \code{include_ejscreen_export} is \code{TRUE}: it calls
\code{\link[=calc_ejscreen_threshold_layers_from_exports]{calc_ejscreen_threshold_layers_from_exports()}} on the national and state
exports and saves them under the registered stage names
\code{ejscreen_threshold_us_ejindexes}, \code{ejscreen_threshold_us_supplemental},
\code{ejscreen_threshold_state_ejindexes}, and
\code{ejscreen_threshold_state_supplemental}. (Issue #395 part (a) — the US/State
percentile CSVs — is the existing \code{ejscreen_export} / \code{ejscreen_export_statepct};
part (c) — the ACS-by-geography layers — is \code{\link[=calc_acs_by_geography]{calc_acs_by_geography()}}.)
}
\seealso{
\code{\link[=colcounter]{colcounter()}} \code{\link[=calc_ejscreen_export]{calc_ejscreen_export()}} \code{\link[=calc_ejscreen_dataset]{calc_ejscreen_dataset()}}
}
\keyword{internal}
