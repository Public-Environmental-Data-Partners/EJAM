% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_bg_acsdata.R
\name{calc_bg_acsdata}
\alias{calc_bg_acsdata}
\title{Calculate the ACS-derived blockgroup pipeline stage}
\usage{
calc_bg_acsdata(
  yr,
  formulas = EJAM::formulas_ejscreen_acs$formula,
  tables = as.vector(EJAM::tables_ejscreen_acs),
  include_tract_data = TRUE,
  tract_tables = c("B18101", "C16001", "B27010"),
  tract_formulas = NULL,
  tract_weight_source = c("decennial2020", "acs"),
  dropMOE = TRUE,
  acs_raw = NULL,
  acs_raw_stage = NULL,
  pipeline_dir = NULL,
  save_stage = FALSE,
  stage_format = c("csv", "rds", "rda", "arrow"),
  overwrite = TRUE,
  validation_strict = TRUE
)
}
\arguments{
\item{yr}{end year of the ACS 5-year survey to use.}

\item{formulas}{formulas used for blockgroup-resolution ACS tables.}

\item{tables}{ACS tables to inspect and download when available at
blockgroup resolution.}

\item{include_tract_data}{logical, whether to add tract-resolution ACS
indicators apportioned to blockgroups.}

\item{tract_tables}{ACS tables to obtain at tract resolution and apportion
to blockgroups.}

\item{tract_formulas}{formulas used for tract-resolution ACS indicators.
Defaults to \code{\link[=calc_blockgroupstats_from_tract_data]{calc_blockgroupstats_from_tract_data()}} defaults.}

\item{tract_weight_source}{source for tract-to-blockgroup apportionment
weights. \code{"decennial2020"} matches the legacy EJSCREEN method by using
2020 Decennial Census blockgroup population weights. \code{"acs"} uses
same-vintage ACS blockgroup population weights.}

\item{dropMOE}{logical, whether to drop ACS margin-of-error columns.}

\item{acs_raw}{optional raw ACS pipeline object from \code{\link[=download_bg_acs_raw]{download_bg_acs_raw()}}.}

\item{acs_raw_stage}{optional stage name to read from \code{pipeline_dir}.}

\item{pipeline_dir}{folder for saving the pipeline stage.}

\item{save_stage}{logical, whether to save the \code{bg_acsdata} stage.}

\item{stage_format}{file format for saved stages: \code{"csv"}, \code{"rds"},
\code{"rda"}, or \code{"arrow"}.}

\item{overwrite}{logical, whether to overwrite an existing saved stage.}

\item{validation_strict}{logical passed to \code{\link[=ejscreen_pipeline_save]{ejscreen_pipeline_save()}}.}
}
\value{
data.table, one row per blockgroup.
}
\description{
Calculate the ACS-derived blockgroup pipeline stage
}
\details{
This is the first step in the reusable ACS pipeline for annual
EJSCREEN/EJAM data updates. It downloads blockgroup-resolution ACS tables via
\code{\link[=calc_blockgroupstats_acs]{calc_blockgroupstats_acs()}}, apportions tract-resolution-only ACS
tables to blockgroups with \code{\link[=calc_blockgroupstats_from_tract_data]{calc_blockgroupstats_from_tract_data()}}, merges
those ACS-derived indicators, and can save the validated \code{bg_acsdata} stage.

\code{bg_acsdata} is intentionally limited to data columns that can be created
using only ACS data. "Demographic index" columns are calculated later
by \code{\link[=calc_ejscreen_blockgroupstats]{calc_ejscreen_blockgroupstats()}}, after
\code{bg_envirodata} and extra indicators have been joined, because the
supplemental demographic index needs \code{lowlifex} which is not from the ACS.
}
\keyword{internal}
