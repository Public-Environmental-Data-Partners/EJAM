% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url_ejamapp.R
\name{url_ejamapp}
\alias{url_ejamapp}
\title{Build a URL that launches the live EJAM app, optionally pre-loaded with sites}
\usage{
url_ejamapp(
  sitepoints = NULL,
  lat = NULL,
  lon = NULL,
  fips = NULL,
  shapefile = NULL,
  shape = NULL,
  radius = NULL,
  buffer = NULL,
  handoff = NULL,
  dTolerance = 100,
  baseurl = "https://ejanalysis.com/ejamapp",
  browse = FALSE,
  shp = NULL
)
}
\arguments{
\item{sitepoints}{data.frame with columns lat and lon (alternative to lat/lon)}

\item{lat, lon}{coordinate vector(s) of point(s), each treated as a site}

\item{fips}{vector of FIPS codes, each treated as a separate site}

\item{shapefile}{an sf polygon/multipolygon object (or ready-made GeoJSON text), encoded as simplified GeoJSON}

\item{shape}{alias (synonym) for shapefile}

\item{radius}{analysis radius in miles (the buffer around points or out from polygon edges)}

\item{buffer}{alias (synonym) for radius ("buffer" reads more naturally for FIPS or polygon analysis)}

\item{handoff}{a token previously returned by the EJAM API \code{POST /handoff}.
When supplied, the other site arguments are ignored and the app fetches the
places back from \verb{GET /handoff/<token>} at startup (the scalable path for
many/large polygons).}

\item{dTolerance}{meters tolerance for \code{\link[sf:st_simplify]{sf::st_simplify()}} so a polygon fits in a URL}

\item{baseurl}{base URL of the live EJAM app. The default is a tidy
\code{ejanalysis.com} shortcut. If \code{baseurl} is a redirect/shortcut, the redirect
must forward the query string so the launch parameters reach the app.}

\item{browse}{set TRUE to open the URL in a browser (if interactive)}

\item{shp}{alias (synonym) for shapefile}
}
\value{
URL (character) for the live EJAM app
}
\description{
Construct a deep link to the EJAM web app. With no site arguments it returns
the bare app URL. Given points, FIPS codes, a polygon shapefile, or a handoff
token, it appends launch query parameters that the deployed app reads at
startup to pre-select those places, so it opens ready to run. This is the way
to hand a set of places to an already-running app via a URL (for example, the
EJScreen "Send to EJAM" button); the deploy-time alternative is the parameters
of \code{\link[=ejamapp]{ejamapp()}}.
}
\details{
The query vocabulary matches \code{\link[=url_ejamapi]{url_ejamapi()}}: \code{lat}, \code{lon}, \code{fips},
\code{shape} (GeoJSON text), \code{radius}, and \code{handoff}. Only one place-type is used
per launch (points, else FIPS, else polygons), matching the app's
one-method-per-analysis model. See the launch-URL handler described in the
"Defaults and Custom Settings for the Web App" vignette.

Large or numerous polygons may not fit in a URL. For those, POST them to the
EJAM API \verb{/handoff} endpoint and pass the returned token as \code{handoff}; the app
then fetches the places back from \verb{GET /handoff/<token>} at startup.
}
\examples{
 url_ejamapp()
 url_ejamapp(lat = c(33, 34), lon = c(-112, -114), radius = 3)
 url_ejamapp(fips = c("10001", "10003"))
 url_ejamapp(handoff = "abc123token")

}
\seealso{
\code{\link[=url_ejamapi]{url_ejamapi()}} \code{\link[=ejamapi]{ejamapi()}} \code{\link[=ejamapp]{ejamapp()}} \code{\link[=url_ejscreenmap]{url_ejscreenmap()}}
}
