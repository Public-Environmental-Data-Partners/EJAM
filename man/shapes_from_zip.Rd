% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes_from_zip.R
\name{shapes_from_zip}
\alias{shapes_from_zip}
\title{Get boundaries of zip codes (ZCTA polygons), to analyze or map zip codes}
\usage{
shapes_from_zip(zipcode, year = NULL, ...)
}
\arguments{
\item{zipcode}{vector of one or more 5-digit zip codes, as numbers or text
(leading zeroes are restored if missing, so 1001 means zip code 01001,
and a ZIP+4 like "10605-1234" is truncated to its first 5 digits)}

\item{year}{optional year of ZCTA vintage, passed to \code{\link[tigris:zctas]{tigris::zctas()}}.
If NULL (default), the tigris package default is used.}

\item{...}{passed to \code{\link[tigris:zctas]{tigris::zctas()}}}
}
\value{
A spatial data.frame (\link[sf:sf]{sf::sf}) with one row per zip code found
(in the same order as the requested zip codes), including a \code{zip} column.
}
\description{
Downloads Census ZIP Code Tabulation Area (ZCTA) boundaries
for the specified zip code(s), returning polygons that can be analyzed
like any other shapefile, as with \code{ejamit(zipcode = 10605)} which uses
this function. See the Zipcodes article/vignette for more background.
}
\details{
Census uses ZIP Code Tabulation Areas (ZCTAs), which are
generalized areal representations of United States Postal Service (USPS)
ZIP Code service areas -- see \code{help("zctas", package = "tigris")}.
Some zip codes (e.g., ones used only for PO boxes or single buildings)
have no ZCTA polygon, and this function warns about and drops those.

Note the first use can be slow, since \code{\link[tigris:zctas]{tigris::zctas()}} downloads a large
national file, but it is cached locally (see \code{\link[tigris:tigris_cache_dir]{tigris::tigris_cache_dir()}})
so later calls are fast.
}
\examples{
\dontrun{
  shp <- shapes_from_zip(c("10012", "10506"))
  mapfast(shp)
  out <- ejamit(shapefile = shp) # same as out <- ejamit(zipcode = c("10012", "10506"))
  }
}
\seealso{
\code{\link[=ejamit]{ejamit()}} \code{\link[=shapes_from_fips]{shapes_from_fips()}} \code{\link[=shapefile_from_any]{shapefile_from_any()}}
}
