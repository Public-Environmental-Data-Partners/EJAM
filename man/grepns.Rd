% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_grepn.R
\name{grepns}
\alias{grepns}
\title{Count matches for multiple patterns across a character vector}
\usage{
grepns(patterns, x, ignore.case = TRUE, rowperx = FALSE, count1perx = TRUE)
}
\arguments{
\item{patterns}{vector of 1+ patterns to search for, passed one at a time to \code{\link[=gregexec]{gregexec()}}}

\item{x}{a vector of character strings to search within}

\item{ignore.case}{passed to \code{\link[=gregexec]{gregexec()}}}

\item{rowperx}{whether to return a matrix with one row per element of \code{x}
and one column per pattern, or instead return a vector with one element
per pattern summarizing across all of \code{x}}

\item{count1perx}{whether to count only 1 match per pattern per element of x,
or to count all matches (i.e., if there are 3 matches for a pattern in one element of x, count as 1 or 3)}
}
\value{
If \code{rowperx = TRUE}, a matrix with one row per element of \code{x} and
one column per pattern. If \code{rowperx = FALSE}, a vector with one element
per pattern summarizing across all of \code{x}.
}
\description{
Count matches for multiple patterns across a character vector
}
\examples{
 grepn("x", c("0 abc", "1 uppercase X", "1 xyz", "2 xx", "3 x x x"))
 grepn("x", c("0 abc", "1 uppercase X", "1 xyz", "2 xx", "3 x x x"), ignore.case = F)

 grepns(c("x", "y"), c("yes", "yx", "this 1 has some x x xxxxx"))
 grepns(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
         rowperx = T, count1perx=T)
 grepns(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
         rowperx = F, count1perx=T)
 grepns(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
         rowperx = T, count1perx=F)
 grepns(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
         rowperx = F, count1perx=F)

 grepls(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
   rowperx = T)
 grepls(c("hi", "other", 'x'), c("said hi ther hi hi hi e", 'hi', 'another', 'none'),
   rowperx = F)


}
\seealso{
\code{\link[=grepns]{grepns()}} \code{\link[=grepls]{grepls()}} \code{\link[=find_in_files]{find_in_files()}} \code{\link[=found_in_files]{found_in_files()}}
\code{\link[=found_in_N_files_T_times]{found_in_N_files_T_times()}} \code{\link[=grep_lines]{grep_lines()}}
}
\keyword{internal}
