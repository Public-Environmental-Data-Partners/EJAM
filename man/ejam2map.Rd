% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ejam2map.R
\name{ejam2map}
\alias{ejam2map}
\title{Show EJAM results as a map of points}
\usage{
ejam2map(
  ejamitout,
  column_names = "ej",
  launch_browser = TRUE,
  shp = NULL,
  radius = NULL,
  sitenumber = NULL,
  shape = NULL,
  shapefile = NULL,
  buffer = NULL,
  sitenumber_label = NULL
)
}
\arguments{
\item{ejamitout}{output of ejamit()}

\item{column_names}{can be "ej", passed to \code{\link[=mapfast]{mapfast()}}}

\item{launch_browser}{logical optional whether to open the web browser to view the map}

\item{shp}{shapefile it can map if analysis was for polygons, for example}

\item{radius}{optional radius in miles}

\item{sitenumber}{as used in \code{\link[=ejam2report]{ejam2report()}}}

\item{shape}{alias (synonym) for shp}

\item{shapefile}{alias (synonym) for shp}

\item{buffer}{alias (synonym) for radius}

\item{sitenumber_label}{optional, display-only override (a number or short text) of the
site number shown in map popups, in place of the auto-assigned row number/ejam_uniq_id.
Only relevant when mapping a single site -- see \code{\link[=ejam2report]{ejam2report()}}, whose
sitenumber_label parameter this supports.}
}
\value{
like what \code{\link[=mapfastej]{mapfastej()}} returns
}
\description{
Takes the output of ejamit() and uses \code{\link[=mapfastej]{mapfastej()}} to
create a map of the points.
}
\details{
Gets radius by checking ejamitout$results_overall$radius.miles
You can use browse=TRUE to save it as a shareable .html file
and see it in your web browser.
}
\examples{
pts = testpoints_100
mapfast(pts)

# out = ejamit(pts, radius = 1)
out = testoutput_ejamit_100pts_1miles

# See in RStudio viewer pane
ejam2map(out, launch_browser = FALSE)
mapfastej(out$results_bysite[c(12,31),])
\dontrun{

# See in local browser instead
ejam2map(out)

# Open folder where interactive map
#  .html file is saved, so you can share it:
x = ejam2map(out)
fname = map2browser(x)
# browseURL(normalizePath(dirname(fname))) # to open the temp folder
# file.copy(fname, "./map.html") # to copy map file to working directory

}
}
