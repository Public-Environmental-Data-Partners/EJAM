% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ejscreen_export.R
\name{calc_ejscreen_dataset_creator_input}
\alias{calc_ejscreen_dataset_creator_input}
\title{Create the input CSV expected by EPA's EJScreen dataset-creator tool}
\usage{
calc_ejscreen_dataset_creator_input(
  blockgroupstats = NULL,
  pipeline_dir = NULL,
  pipeline_storage = c("auto", "local", "s3"),
  blockgroupstats_stage = "blockgroupstats",
  blockgroupstats_path = NULL,
  stage_format = c("csv", "rds", "rda", "arrow"),
  mapping_for_names = map_headernames,
  rename_newtype = "ejscreen_indicator",
  expected_output_names = ejscreen_dataset_creator_input_fields(),
  placeholder_fields = ejscreen_dataset_creator_placeholder_fields(),
  force_placeholder_fields = ejscreen_dataset_creator_placeholder_fields(),
  fill_missing = TRUE,
  return_report = FALSE,
  save_stage = FALSE,
  save_path = NULL,
  save_format = NULL,
  overwrite = TRUE,
  validation_strict = TRUE
)
}
\arguments{
\item{blockgroupstats}{blockgroupstats-like data.frame, or NULL if reading
from a saved pipeline stage.}

\item{pipeline_dir}{folder for reading/saving pipeline stages.}

\item{pipeline_storage}{stage storage backend: \code{"auto"}, \code{"local"}, or
\code{"s3"}.}

\item{blockgroupstats_stage}{stage name to read when \code{blockgroupstats} is not
supplied.}

\item{blockgroupstats_path}{explicit path to a saved blockgroupstats input.}

\item{stage_format}{input/output stage file format.}

\item{mapping_for_names}{map_headernames-like crosswalk.}

\item{rename_newtype}{target naming column in \code{mapping_for_names}.}

\item{expected_output_names}{final EJScreen field names to create and order.}

\item{placeholder_fields}{fields that may be created as explicit \code{NA}
placeholders if unavailable.}

\item{force_placeholder_fields}{fields to write as explicit \code{NA}
placeholders even if a mapped source column is present. This defaults to
post-percentile exceedance-count fields, because those are not really
pre-index inputs for the EJScreen Python process.}

\item{fill_missing}{logical. If TRUE, add unavailable expected fields as
\code{NA} columns and report them. If FALSE, stop when any expected field is
unavailable.}

\item{return_report}{logical. If TRUE, return a list with \code{data} and
\code{report}.}

\item{save_stage}{logical. If TRUE, save as the
\code{ejscreen_dataset_creator_input} pipeline stage.}

\item{save_path}{optional direct path to save the input table.}

\item{save_format}{optional direct-save format, guessed from \code{save_path} when
NULL.}

\item{overwrite}{logical. If FALSE, refuse to overwrite saved output.}

\item{validation_strict}{logical passed to \code{\link[=ejscreen_pipeline_save]{ejscreen_pipeline_save()}}.}
}
\value{
data.frame, or a list with \code{data} and \code{report} when
\code{return_report = TRUE}.
}
\description{
Create the input CSV expected by EPA's EJScreen dataset-creator tool
}
\details{
This helper prepares the smaller pre-index input table expected by
\code{ejscreen-dataset-creator-2.3}. It is intended for the alternative workflow
where EJAM creates the ACS/environmental/extra-indicator base table, but the
EJScreen Python scripts calculate EJ indexes, percentiles, map bins, and map
popup text.

The helper reads a \code{blockgroupstats}-like object or saved pipeline stage,
renames fields through \link{map_headernames} via the same \code{ejscreen_indicator}
metadata used by \code{\link[=fixcolnames]{fixcolnames()}}, adds explicit placeholder columns where
requested fields are not available, and returns columns in the order expected
by the Python tool's \code{col_names.py}.

A report is attached as the attribute
\code{ejscreen_dataset_creator_input_report}. Set \code{return_report = TRUE} to
return both the data and report.
}
\examples{
\dontrun{
# Create the pre-index input CSV that EPA's ejscreen-dataset-creator-2.3
# Python tool expects, using pipeline files that already exist on S3.
pipeline_dir <- paste0(
  "s3://pedp-data-preserved/ejscreen-data-processing/pipeline/",
  "ejscreen_acs_2024"
)

out <- EJAM:::calc_ejscreen_dataset_creator_input(
  pipeline_dir = pipeline_dir,
  pipeline_storage = "s3",
  stage_format = "csv",
  save_stage = TRUE,
  return_report = TRUE
)

# This writes the file here:
EJAM:::ejscreen_pipeline_stage_path(
  "ejscreen_dataset_creator_input",
  pipeline_dir = pipeline_dir,
  format = "csv"
)

# Review any fields that were filled rather than mapped from blockgroupstats.
subset(out$report, status \%in\% c("placeholder", "missing_filled"))
}

}
\keyword{internal}
