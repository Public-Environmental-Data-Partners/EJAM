% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ejscreen_pipeline_validate_replicates_prior.R
\name{ejscreen_pipeline_compare_stage_to_git_ref}
\alias{ejscreen_pipeline_compare_stage_to_git_ref}
\title{Compare one saved pipeline stage to a data object at an explicit Git ref}
\usage{
ejscreen_pipeline_compare_stage_to_git_ref(
  stage,
  git_ref,
  git_path = "data/blockgroupstats.rda",
  object_name = NULL,
  new_dt = NULL,
  new_pipeline_dir = NULL,
  new_stage = stage,
  format = "csv",
  storage = c("auto", "local", "s3"),
  shared_only = FALSE,
  id_cols = "bgfips",
  output_dir = NULL,
  write_files = FALSE,
  use_waldo = FALSE
)
}
\arguments{
\item{stage}{label to use in summaries.}

\item{git_ref}{Git branch, tag, or commit SHA holding the prior/reference
package data.}

\item{git_path}{Repository path to the \code{.rda} prior/reference file.}

\item{object_name}{Optional object name inside \code{git_path}.}

\item{new_dt}{Optional new data object. If NULL, \code{new_stage} is loaded from
\code{new_pipeline_dir}.}

\item{new_pipeline_dir}{Folder or S3 prefix holding the new pipeline stage
file. Required when \code{new_dt} is NULL.}

\item{new_stage}{Stage name to load from \code{new_pipeline_dir}. Defaults to
\code{stage}.}

\item{format}{File format used when loading the saved new pipeline stage.}

\item{storage}{Storage backend: \code{"auto"}, \code{"local"}, or \code{"s3"}.}

\item{shared_only}{Logical. If TRUE, compare only prior columns shared with
the new data, plus \code{id_cols}.}

\item{id_cols}{Identifier columns to keep for shared-column comparisons.}

\item{output_dir}{Optional folder or S3 prefix for written validation
artifacts.}

\item{write_files}{Logical. If TRUE, write one detail text file and one
one-row CSV summary for this stage.}

\item{use_waldo}{Logical passed to \code{\link[=ejscreen_pipeline_validate_vs_prior]{ejscreen_pipeline_validate_vs_prior()}}.}
}
\value{
List with \code{result}, \code{summary}, \code{text}, \code{warnings}, and \code{error}.
}
\description{
Compare one saved pipeline stage to a data object at an explicit Git ref
}
\examples{
\dontrun{
# compare s3 copy of 2022 blockgroupstats.csv
# to v2.32.8.001 release version of blockgroupstats.rda

x <- EJAM:::ejscreen_pipeline_compare_stage_to_git_ref(
  git_ref = "v2.32.8.001",
  git_path = "data/blockgroupstats.rda",
  stage = "blockgroupstats",
  new_pipeline_dir = paste0(
    "s3://pedp-data-preserved/ejscreen-data-processing/pipeline/",
    "ejscreen_acs_2022"
  )
)
print(x$text)
x$result$not_replicated

# compare only the 13 environmental indicators in names_e
# from a user-provided file against the v2.32.8.001 package data
user_env <- data.table::fread("path/to/user_bg_envirodata.csv")
user_env <- user_env[, c("bgfips", EJAM::names_e), with = FALSE]
x_env <- EJAM:::ejscreen_pipeline_compare_stage_to_git_ref(
  git_ref = "v2.32.8.001",
  git_path = "data/blockgroupstats.rda",
  stage = "bg_envirodata_names_e",
  new_dt = user_env,
  shared_only = TRUE,
  id_cols = "bgfips"
)
print(x_env$text)
}

}
\keyword{internal}
