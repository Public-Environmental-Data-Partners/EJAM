% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ejscreen_names.R
\name{validate_map_headernames_ejscreen_names}
\alias{validate_map_headernames_ejscreen_names}
\title{Validate EJSCREEN name columns in map_headernames}
\usage{
validate_map_headernames_ejscreen_names(
  mapping_for_names = map_headernames,
  strict = FALSE,
  source_name = "map_headernames"
)
}
\arguments{
\item{mapping_for_names}{a data.frame like \link{map_headernames}.}

\item{strict}{logical. If \code{TRUE}, validate the complete committed
\code{map_headernames} source, including required columns, schema rows, and
bin/text helper rows.}

\item{source_name}{character label used in error messages.}
}
\value{
The input as a plain data.frame. No metadata rows or columns are
added, removed, or changed.
}
\description{
Validate EJSCREEN name columns in map_headernames
}
\details{
\code{map_headernames} has historically kept several naming systems:
\code{rname} for EJAM, \code{acsname} for ACS-derived variables,
\code{ejscreen_apinames_old} for the old offline EJSCREEN report/API names, and
\code{csvname} for the older EJSCREEN staff CSV/FTP-style download fields. Current
EJSCREEN map services use geodatabase/download field names for numeric
fields, plus related \code{P_}, \code{B_}, and \code{T_} fields for percentiles, map bins,
and popup text. Percentile, map-bin, and popup-text fields are represented as
their own rows, with \code{rname} values such as \code{pctile.pm}, \code{bin.pm}, and
\code{text.pm}.

\code{data-raw/map_headernames.csv} is now the authoritative editable source for
this metadata. Build scripts should read that CSV, validate it, and save
\code{data/map_headernames.rda} without creating or changing metadata rows in
code. This validator exists to keep export code explicit about the metadata
it requires; it should not be used as a hidden augmentation step.

The \code{ejscreen_ftp_names} values are intended to preserve the field names used
in EPA's old EJSCREEN FTP/download CSV and geodatabase files, such as the
archived 2024 v2.32 block-group files and the accompanying
\code{EJScreen_2024_BG_Percentiles_Columns.xlsx} and
\code{EJScreen_2024_BG_State_Percentiles_Columns.xlsx} column dictionaries. Those
names are usually the same as \code{ejscreen_indicator}, but both columns are kept so
old FTP/download provenance and current app/export naming can diverge later
if needed.
}
\keyword{internal}
