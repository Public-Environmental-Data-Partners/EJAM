% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape2geojson.R
\name{shape2geojson}
\alias{shape2geojson}
\title{convert spatial data.frame to a vector of geojson text strings}
\usage{
shape2geojson(
  shp,
  file = file.path(tempdir(), "shp.geojson"),
  txt = TRUE,
  combine_in_one_string = FALSE,
  combine_in_one_file = TRUE
)
}
\arguments{
\item{shp}{spatial data.frame to be written via \code{\link[sf:st_write]{sf::st_write()}}}

\item{file}{optional file path and name, useful if txt=F}

\item{txt}{optional logical, set to FALSE to just get the path to a temp .geojson file}

\item{combine_in_one_string}{set to TRUE to get back only 1 geojson txt string.
If FALSE, output is a vector of strings.}

\item{combine_in_one_file}{set to TRUE to get back only 1 file (1 row per polygon, not union/dissolved).
If FALSE, output is a vector of filenames (saves each row of input shp as a separate file).}
}
\value{
if txt=T, returns geojson text string(s) for the input spatial data.frame
if txt=F, returns file path/name(s) of .geojson file(s).
}
\description{
convert spatial data.frame to a vector of geojson text strings
}
\details{
helper for \code{\link[=url_ejamapi]{url_ejamapi()}}
Note it removes all spaces in the string.

Note that trying to use txt=T and combine_in_one_string = T for large polygons or many polygons
would create a very long string that might exceed URL length limits for GET requests,
if that is what you're using the text for.
}
\examples{
shp =  testinput_shapes_2[2, c("geometry", "FIPS")]
x = shape2geojson(shp)
nchar(x)

}
\seealso{
\code{\link[geojsonsf:sf_geojson]{geojsonsf::sf_geojson()}} that should be able to do the same as shape2geojson().
Also, \code{\link[geojsonsf:geojson_sf]{geojsonsf::geojson_sf()}} that does the inverse, converts geojson to sf.
Also see \code{\link[=shapefile_from_any]{shapefile_from_any()}} that converts text in geojson format to sf.
}
