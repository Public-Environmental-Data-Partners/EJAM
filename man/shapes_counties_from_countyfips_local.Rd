% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapes_from_fips.R
\name{shapes_counties_from_countyfips_local}
\alias{shapes_counties_from_countyfips_local}
\title{Get County boundaries from the \link{counties_shapefile} dataset built into this package}
\usage{
shapes_counties_from_countyfips_local(fips)
}
\arguments{
\item{fips}{vector of County FIPS codes, already zero-padded and validated}
}
\value{
spatial data.frame of boundaries, shaped like the download path's output,
or NULL if the built-in dataset cannot serve every requested FIPS
}
\description{
Get County boundaries from the \link{counties_shapefile} dataset built into this package
}
\details{
Mirrors what \code{\link[=shapes_state_from_statefips]{shapes_state_from_statefips()}} does for States.

\link{counties_shapefile} holds the Census Bureau cartographic boundary file, which
is the same source and resolution the \code{myservice = 'cartographic'} download
path returns (tidycensus/tigris with \code{cb = TRUE}), so using it avoids a
network round-trip without changing which boundaries are used.

This returns NULL rather than partial results if any requested FIPS is not
in the built-in dataset, so that the caller can fall back to downloading
and no request is ever silently served with some counties missing.
}
\seealso{
\code{\link[=shapes_from_fips]{shapes_from_fips()}} \code{\link[=shapes_counties_from_countyfips]{shapes_counties_from_countyfips()}} \link{counties_shapefile}
}
\keyword{internal}
