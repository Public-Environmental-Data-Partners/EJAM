% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ejscreen_pctile_lookup_export.R
\name{calc_ejscreen_pctile_lookup_export}
\alias{calc_ejscreen_pctile_lookup_export}
\title{Create EJScreen-style percentile lookup exports}
\usage{
calc_ejscreen_pctile_lookup_export(
  lookup,
  values = NULL,
  scope = c("national", "state"),
  output_fields = ejscreen_pctile_lookup_fields(),
  mapping_for_names = map_headernames,
  rename_newtype = "ejscreen_indicator",
  include_std = TRUE,
  save_path = NULL,
  save_format = NULL,
  pipeline_storage = c("auto", "local", "s3"),
  overwrite = TRUE
)
}
\arguments{
\item{lookup}{\code{usastats}- or \code{statestats}-like lookup table.}

\item{values}{optional raw-score data used to calculate \code{std} rows.}

\item{scope}{\code{"national"} for a USA lookup or \code{"state"} for state lookups.}

\item{output_fields}{optional EJScreen output fields. Defaults to the fields
used by EPA's archived national/state lookup CSVs.}

\item{mapping_for_names}{map_headernames-like crosswalk.}

\item{rename_newtype}{target EJScreen naming column in \code{mapping_for_names}.}

\item{include_std}{logical. If TRUE, append or replace one \code{std} row per
region. When \code{values} is NULL, \code{std} rows are included with missing values.}

\item{save_path}{optional path where the export should be written.}

\item{save_format}{optional file format. Guessed from \code{save_path} when NULL.}

\item{pipeline_storage}{storage backend: \code{"auto"}, \code{"local"}, or \code{"s3"}.}

\item{overwrite}{logical. If FALSE, refuse to overwrite \code{save_path}.}
}
\value{
data.frame with EJScreen lookup CSV columns.
}
\description{
Create EJScreen-style percentile lookup exports
}
\details{
This helper converts EJAM percentile lookup tables such as
\link{usastats} and \link{statestats} to the EJScreen CSV shape used by files such as
\code{EJScreen_2024_BG_National_Lookup.csv} and
\code{EJScreen_2024_BG_State_Lookup.csv}. The package lookup tables keep EJAM
\code{rname} columns and include \code{0:100} percentile rows plus \code{mean}. This export
writes \code{PCTILE} then \code{REGION}, renames indicator columns to EJScreen field
names, and can append a \code{std} row after the \code{mean} row for each region.

The \code{std} row is calculated from \code{values}, not from the percentile lookup
table itself. For the annual pipeline, \code{values} is the matching
\code{blockgroupstats} plus \code{bgej} raw-score table.
}
\keyword{internal}
