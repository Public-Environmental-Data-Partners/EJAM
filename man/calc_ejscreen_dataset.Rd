% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ejscreen_dataset.R
\name{calc_ejscreen_dataset}
\alias{calc_ejscreen_dataset}
\title{Run the staged EJSCREEN/EJAM dataset update pipeline}
\usage{
calc_ejscreen_dataset(
  yr,
  bg_envirodata = NULL,
  bg_extra_indicators = NULL,
  bg_geodata = NULL,
  bg_acs_raw = NULL,
  bg_acsdata = NULL,
  blockgroupstats = NULL,
  pipeline_dir = NULL,
  pipeline_storage = c("auto", "local", "s3"),
  save_stages = FALSE,
  use_saved_stages = TRUE,
  stage_format = c("csv", "rds", "rda", "arrow"),
  raw_acs_storage = c("folder", "object"),
  raw_table_format = stage_format,
  overwrite = TRUE,
  validation_strict = TRUE,
  download_acs_raw = TRUE,
  acs_download_fun = ACSdownload::get_acs_new,
  return_intermediate = TRUE,
  include_ejscreen_export = FALSE,
  include_ejscreen_dataset_creator_input = FALSE,
  ejscreen_export_path = NULL,
  ejscreen_dataset_creator_input_path = NULL,
  ejscreen_export_vars = NULL,
  ejscreen_export_required_names = NULL,
  ejscreen_export_rename_newtype = "ejscreen_indicator",
  ejscreen_export_feature_server_fields = NULL,
  blockgroup_tables = setdiff(as.vector(EJAM::tables_ejscreen_acs), tract_tables),
  tract_tables = c("B18101", "C16001", "B27010"),
  tract_weight_source = c("decennial2020", "acs"),
  include_tract_data = TRUE,
  fiveorone = "5",
  download_timeout = 3600,
  download_retries = 2,
  download_bg_geodata = FALSE,
  blockgroup_universe_source = c("acs", "union"),
  formulas = EJAM::formulas_ejscreen_acs$formula,
  tract_formulas = NULL,
  dropMOE = TRUE,
  extra_indicator_vars = ejscreen_default_extra_indicator_vars(),
  reuse_existing_if_missing = FALSE,
  existing_blockgroupstats = NULL,
  acs_vars = NULL,
  enviro_vars = NULL,
  ej_indicator_vars = names_e,
  ej_indicator_pctile_vars = names_e_pctile,
  ej_indicator_state_pctile_vars = names_e_state_pctile,
  ej_index_vars = names_ej,
  ej_index_supp_vars = names_ej_supp,
  ej_index_state_vars = names_ej_state,
  ej_index_supp_state_vars = names_ej_supp_state,
  demog_index_var = "Demog.Index",
  demog_index_supp_var = "Demog.Index.Supp",
  demog_index_state_var = "Demog.Index.State",
  demog_index_supp_state_var = "Demog.Index.Supp.State"
)
}
\arguments{
\item{yr}{end year of the ACS 5-year survey to use.}

\item{bg_envirodata}{environmental blockgroup table. If NULL, the wrapper
tries to read the saved \code{bg_envirodata} stage when \code{use_saved_stages} is
TRUE.}

\item{bg_extra_indicators}{non-ACS, non-enviro blockgroup indicators such as
\code{lowlifex}, or NULL to read/reuse/create that stage.}

\item{bg_geodata}{Census/TIGER blockgroup geography stage, with
square-meter \code{arealand} and \code{areawater} fields.}

\item{bg_acs_raw}{optional raw ACS pipeline object from
\code{\link[=download_bg_acs_raw]{download_bg_acs_raw()}}.}

\item{bg_acsdata}{optional ACS-derived blockgroup table from
\code{\link[=calc_bg_acsdata]{calc_bg_acsdata()}}.}

\item{blockgroupstats}{optional already-combined blockgroupstats-like table.}

\item{pipeline_dir}{folder or \verb{s3://...} URI for reading/writing pipeline
stage files.}

\item{pipeline_storage}{stage storage backend: \code{"auto"}, \code{"local"}, or
\code{"s3"}. \code{"auto"} uses S3 when \code{pipeline_dir} starts with \verb{s3://} and local
file storage otherwise.}

\item{save_stages}{logical, whether to save each stage as it is created.}

\item{use_saved_stages}{logical, whether missing inputs may be read from
existing files in \code{pipeline_dir}.}

\item{stage_format}{file format for saved/read tabular stages: \code{"csv"},
\code{"rds"}, \code{"rda"}, or \code{"arrow"}. The wrapper defaults to CSV so every
pipeline checkpoint is easy to inspect outside R.}

\item{raw_acs_storage}{raw ACS checkpoint storage pattern. \code{"folder"} saves
one ACS table per file plus a manifest. \code{"object"} saves the historical
single \code{bg_acs_raw} list object.}

\item{raw_table_format}{file format for per-table raw ACS files when
\code{raw_acs_storage = "folder"}.}

\item{overwrite}{logical, whether to overwrite saved stage files.}

\item{validation_strict}{logical passed to stage validators.}

\item{download_acs_raw}{logical, whether to download raw ACS tables when
neither \code{bg_acsdata} nor saved ACS stages are available.}

\item{acs_download_fun}{ACSdownload-compatible function used by
\code{\link[=download_bg_acs_raw]{download_bg_acs_raw()}} when raw ACS tables need to be downloaded. The
default is \code{\link[ACSdownload:get_acs_new]{ACSdownload::get_acs_new()}}. Supply a wrapper if you need a
legacy ACS source implementation.}

\item{return_intermediate}{logical. If TRUE, return key interim stage objects
in addition to final datasets.}

\item{include_ejscreen_export}{logical. If TRUE, also create an
EJSCREEN-ready export using \code{\link[=calc_ejscreen_export]{calc_ejscreen_export()}}.}

\item{include_ejscreen_dataset_creator_input}{logical. If TRUE, also create
the smaller pre-index input table expected by EPA's
\code{ejscreen-dataset-creator-2.3} Python tool.}

\item{ejscreen_export_path}{optional file path for the EJSCREEN export.}

\item{ejscreen_dataset_creator_input_path}{optional file path for the
EJScreen dataset-creator input table.}

\item{ejscreen_export_vars}{optional EJAM \code{rname} columns to keep in the
EJSCREEN export before renaming.}

\item{ejscreen_export_required_names}{optional final EJSCREEN field names
that must be present.}

\item{ejscreen_export_rename_newtype}{naming column in \link{map_headernames} to
use when renaming the EJSCREEN export.}

\item{ejscreen_export_feature_server_fields}{optional final EJSCREEN
FeatureServer field list. Defaults to the current EJSCREEN v2.32 block
group FeatureServer schema when an EJSCREEN export is requested.}

\item{blockgroup_tables}{ACS tables to download at blockgroup resolution.}

\item{tract_tables}{ACS tables to download at tract resolution for later
blockgroup apportionment.}

\item{tract_weight_source}{source for tract-to-blockgroup apportionment
weights. \code{"decennial2020"} matches the legacy EJSCREEN method by using
2020 Decennial Census blockgroup population weights. \code{"acs"} uses
same-vintage ACS blockgroup population weights.}

\item{include_tract_data}{logical, whether to download \code{tract_tables}.}

\item{fiveorone}{ACS sample length, \code{"5"} by default.}

\item{download_timeout}{timeout in seconds to use while downloading ACS
table files. This is increased above R's usual 60 second default because
some Census table-based summary files are hundreds of MB.}

\item{download_retries}{number of times to retry a failed ACS table download
after the initial attempt.}

\item{download_bg_geodata}{logical, whether to download Census/TIGER
blockgroup geography when \code{bg_geodata} is not supplied or saved.}

\item{blockgroup_universe_source}{passed to \code{\link[=calc_ejscreen_blockgroupstats]{calc_ejscreen_blockgroupstats()}}.
The default \code{"acs"} uses the ACS table rows as the authoritative
blockgroup universe for the requested ACS vintage.}

\item{formulas}{formulas used for blockgroup-resolution ACS tables.}

\item{tract_formulas}{formulas used for tract-resolution ACS indicators.
Defaults to \code{\link[=calc_blockgroupstats_from_tract_data]{calc_blockgroupstats_from_tract_data()}} defaults.}

\item{dropMOE}{logical, whether to drop ACS margin-of-error columns.}

\item{extra_indicator_vars}{expected extra indicator columns.}

\item{reuse_existing_if_missing}{logical, whether missing extra indicators
should be copied from \code{existing_blockgroupstats}.}

\item{existing_blockgroupstats}{optional blockgroupstats-like table to use
when \code{reuse_existing_if_missing} is TRUE. Defaults to current package data.}

\item{acs_vars}{variables to include in the ACS-only lookup stages. Defaults
to current EJSCREEN/EJAM ACS indicators found in \code{bgstats}.}

\item{enviro_vars}{variables to include in the environmental lookup stages.
Defaults to current environmental, health, site, climate, and feature
variables found in \code{bgstats}.}

\item{ej_indicator_vars}{environmental indicators to use when calculating EJ
indexes. Defaults to \link{names_e}, but can be replaced for custom indicators.}

\item{ej_indicator_pctile_vars, ej_indicator_state_pctile_vars}{names for
national/state environmental percentile columns used internally by
\code{\link[=calc_bgej]{calc_bgej()}}.}

\item{ej_index_vars, ej_index_supp_vars, ej_index_state_vars, ej_index_supp_state_vars}{names for the four EJ-index families created by \code{\link[=calc_bgej]{calc_bgej()}}.}

\item{demog_index_var, demog_index_supp_var, demog_index_state_var, demog_index_supp_state_var}{demographic index column names used by \code{\link[=calc_bgej]{calc_bgej()}}.}
}
\value{
named list containing final datasets (\code{blockgroupstats}, \code{bgej},
\code{usastats}, and \code{statestats}) plus interim stages when
\code{return_intermediate} is TRUE. Attributes record \code{pipeline_dir},
\code{stage_format}, and saved stage paths.
}
\description{
Run the staged EJSCREEN/EJAM dataset update pipeline
}
\details{
\code{calc_ejscreen_dataset()} can be called from a script each year.
See the runner script \code{data-raw/run_ejscreen_acs2024_pipeline.R}
for an example of how to call this function, and how it can be used
to run the whole pipeline from start to finish with minimal manual intervention.

\code{calc_ejscreen_dataset()} is a high-level wrapper around the staged
annual update helpers. It is intentionally an orchestrator rather than a
replacement for the individual stage functions. Each major input or output can
be supplied as an R object, read from a saved stage in \code{pipeline_dir}, or
created and saved by this function.

The default stage order is:
\enumerate{
\item download raw ACS tables of demographic data into \code{bg_acs_raw}
\item calculate ACS-based demographic indicators (and lead paint indicator) as \code{bg_acsdata}
\item validate/save \code{bg_envirodata} (key environmental indicators)
\item validate/save \code{bg_extra_indicators} (e.g., \% low life expectancy)
\item create or validate \code{bg_geodata}, the Census/TIGER blockgroup geography
attributes used for \code{arealand}, \code{areawater}, and internal-point fields
\item calculate demographic indexes (using \% low life expectancy, etc.)
\item combine those blockgroup demog., envt., extra, and geography indicators
as \link{blockgroupstats}
\item create intermediate percentile lookup tables \code{usastats_acs},
\code{statestats_acs}, \code{usastats_envirodata}, \code{statestats_envirodata}
\item calculate EJ indexes (from envt. percentiles and demog. indexes) and save
as \link{bgej} table
\item create intermediate percentile lookup tables \code{usastats_ej},
\code{statestats_ej}
\item combine those as \link{usastats} and \link{statestats}
\item create an EJScreen-ready export file (optionally)
}

\code{bg_envirodata} must include \code{pctpre1960}. That column may be produced by an
upstream environmental-data step that reads the saved \code{bg_acsdata} stage.

The annual pipeline creates the \code{bgej} stage, and the package-level dynamic
Arrow loader obtains \code{bgej.arrow} from the \code{ejamdata} release that matches
the EJAM package version currently in use. For example, when
\code{packageVersion("EJAM")} reports \verb{2.5.0}, \code{\link[=dataload_dynamic]{dataload_dynamic()}} and
\code{\link[=download_latest_arrow_data]{download_latest_arrow_data()}} look for \code{bgej.arrow} in the \code{ejamdata}
release tagged \code{v2.5.0}, not in the latest data-repository release.

Note that the runner script can use several settings stored as environment variables:
\itemize{
\item EJAM_PIPELINE_YR
\item EJAM_PIPELINE_DIR: override output folder.
\item EJAM_PIPELINE_STORAGE: auto, local, or s3. auto treats s3:// paths as S3.
\item EJAM_STAGE_FORMAT: primary stage format used for loading, usually csv.
\item EJAM_STAGE_FORMATS: comma-separated formats saved by the runner, usually csv,rda.
\item EJAM_BLOCKGROUP_UNIVERSE_SOURCE: acs or union. acs is recommended.
\item EJAM_TRACT_WEIGHT_SOURCE: decennial2020 or acs. decennial2020 matches
legacy EJSCREEN tract-to-blockgroup apportionment.
\item AWS_PROFILE and AWS_REGION: used when pipeline_storage is s3
\item CENSUS_API_KEY: used by functions that download ACS data (or that download boundaries/shapefiles for FIPS from some sources)
\item EJAM_FORCE_ACS: TRUE to redownload/recalculate raw ACS and bg_acsdata.
\item EJAM_FORCE_BG_ACSDATA: TRUE to rebuild bg_acsdata from saved raw ACS.
\item EJAM_FORCE_BG_GEODATA: TRUE to redownload/recalculate Census/TIGER blockgroup geodata.
\item EJAM_ACS_DOWNLOAD_TIMEOUT
\item EJAM_ACS_DOWNLOAD_RETRIES
\item EJAM_USE_PROVISIONAL_BG_ENVIRODATA: FALSE to require bg_envirodata.csv.
\item EJAM_INCLUDE_EJSCREEN_EXPORT: TRUE to create ejscreen_export.csv.
\item EJAM_VALIDATE_VS_PRIOR and related EJAM_PRIOR_* settings control prior-version comparisons.
}

The annual runner also writes \code{pipeline_run_manifest.csv}, which records the
package version, Git branch/SHA, ACS vintage, run settings, and whether
provisional environmental or extra-indicator inputs were reused.

Census/TIGER geography can occasionally include valid blockgroup features
that are not present in the ACS summary-file tables for the same ACS vintage.
For example, a draft ACS 2020-2024 build found 39 Suffolk County, New York
blockgroups in TIGER geography but not in the relevant ACS blockgroup or
tract tables. The default \code{blockgroup_universe_source = "acs"} therefore
treats \code{bg_acsdata} as the authoritative final blockgroup universe and uses
\code{bg_geodata} only to annotate those rows.

To check them:

\if{html}{\out{<div class="sourceCode">}}\preformatted{print(
cbind(current_setting = Sys.getenv(c(
  "EJAM_PIPELINE_YR",
  "EJAM_PIPELINE_DIR", "EJAM_PIPELINE_STORAGE",
  "EJAM_STAGE_FORMAT", "EJAM_STAGE_FORMATS",
  "EJAM_BLOCKGROUP_UNIVERSE_SOURCE",
  "AWS_PROFILE", "AWS_REGION",
  "CENSUS_API_KEY",
  "EJAM_FORCE_ACS", "EJAM_FORCE_BG_ACSDATA", "EJAM_FORCE_BG_GEODATA",
  "EJAM_ACS_DOWNLOAD_TIMEOUT", "EJAM_ACS_DOWNLOAD_RETRIES",
  "EJAM_USE_PROVISIONAL_BG_ENVIRODATA",
  "EJAM_INCLUDE_EJSCREEN_EXPORT",
  "EJAM_VALIDATE_VS_PRIOR", "EJAM_PRIOR_PIPELINE_YR",
  "EJAM_PRIOR_PIPELINE_DIR", "EJAM_PRIOR_PACKAGE_REF"
)))
)
}\if{html}{\out{</div>}}
}
\keyword{internal}
