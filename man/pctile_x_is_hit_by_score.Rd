% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pctile_x_is_hit_by_score.R
\name{pctile_x_is_hit_by_score}
\alias{pctile_x_is_hit_by_score}
\title{Check whether raw scores meet a percentile cutoff (e.g., to see which blockgroups are at high percentiles)}
\usage{
pctile_x_is_hit_by_score(
  raw_score_name,
  cutoff = 0.9,
  score = NULL,
  ST = FALSE
)
}
\arguments{
\item{raw_score_name}{Character scalar. Name of the raw indicator column to
evaluate, such as \code{"pctlowinc"} or \code{"o3"}. It must exist in
\link{usastats} and \link{blockgroupstats}.}

\item{cutoff}{Numeric scalar from 0 to 1. Percentile cutoff expressed as a
fraction, for example \code{0.90} for the 90th percentile.}

\item{score}{Optional numeric vector of raw values to compare to the cutoff.
If omitted or \code{NULL}, the function uses the column named by
\code{raw_score_name} from \link{blockgroupstats}.}

\item{ST}{Either \code{FALSE}/\code{NULL} for nationwide percentiles, \code{TRUE} to use
\code{blockgroupstats$ST}, or a character vector of state abbreviations the same
length as \code{score} for state-specific comparisons.}
}
\value{
Logical vector the same length as \code{score}, indicating if each raw
score is at or above the requested percentile cutoff.
}
\description{
Compares one or more raw indicator values to the raw-score threshold that
corresponds to a specified percentile cutoff in \link{usastats} or \link{statestats}.
}
\details{
To save space, blockgroupstats does not store the US and State percentiles
of all US blockgroups for all indicators -- it just has the raw scores.
Therefore one cannot easily check which blockgroups have raw scores that are
at or above a given percentile cutoff, without first converting the raw scores to percentiles
with \code{\link[=lookup_pctile]{lookup_pctile()}} and then checking which percentiles are at or above the cutoff.
This function does that in a more efficient way by just looking up the raw score
that corresponds to the cutoff percentile in usastats or statestats,
and then comparing all the raw scores to that cutoff raw score.
It is a fast alternative to converting every raw score to a percentile with
\code{\link[=lookup_pctile]{lookup_pctile()}} and then checking whether that percentile is at or above the
requested cutoff.

If \code{score} is omitted, values are taken from \code{blockgroupstats[[raw_score_name]]}.
If \code{ST = TRUE}, the function also takes states from \code{blockgroupstats$ST} and
ignores any user-supplied \code{score}.
}
\examples{
\donttest{

  # Which blockgroups have a raw score at/above a specified raw score cutoff?

   # Which ones have \% unemployed of at least 99\%?
   blockgroupstats[  pctunemployed >= 0.99, 1:12]

 # What \% of blockgroups have a raw score at/above a specified raw score cutoff?

   # What \% of places have \% unemployed of at least 50\%?
   round(table(
     pct.with.at.least.50pct.unemployed =
     blockgroupstats$pctunemployed >= 0.50) /
     nrow(blockgroupstats)*100, 2)


  # Which blockgroups have a raw score at/above a specified percentile?

    these <- pctile_x_is_hit_by_score("pctunemployed", cutoff = 0.95)
    blockgroupstats[these, 1:12]

  # What \% of blockgroups have a raw score at/above a specified percentile?

    these <- pctile_x_is_hit_by_score("pctunemployed", cutoff = 0.95)
    round(table(percent.that.are.high = these)/NROW(these) * 100, 2)
    # About 5\% of US blockgroups are at/above 95th percentile as expected


pctile_x_is_hit_by_score("pctlowinc", cutoff = 0.80, score = c(0.1, 0.33, 0.50))

testrows <- c(14840L, 96520L, 105100L)
pctile_x_is_hit_by_score(
  "pctlowinc",
  cutoff = 0.80,
  score = EJAM::blockgroupstats$pctlowinc[testrows],
  ST = EJAM::blockgroupstats$ST[testrows]
)
blockgroupstats[testrows, .(bgfips, ST, pop, pctlowinc)]

}

}
\seealso{
\code{\link[=lookup_pctile]{lookup_pctile()}} \code{\link[=pctile_from_raw_lookup]{pctile_from_raw_lookup()}} \code{\link[=calc_pctile_columns]{calc_pctile_columns()}}
}
\keyword{internal}
